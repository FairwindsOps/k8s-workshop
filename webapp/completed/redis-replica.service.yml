apiVersion: v1
kind: Service
metadata:
  name: redis-replica
  namespace: k8s-workshop
  labels:
    app: redis
    role: replica
    tier: backend
spec:
  ports:
  - port: 6379
  selector:
    app: redis
    role: replica
    tier: backend
